# 安全策略

## 支持范围

安全修复优先应用于最新发布版本和 `main` 分支。旧版本可能只获得升级建议。

## 报告方式

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密提交功能。若仓库尚未启用 Security Advisory，请私下联系仓库维护者，不要在公开 Issue 中发布以下内容：

- 能绕过 STOP、DISABLE、闭环预检或 CAN 会话隔离的步骤；
- 可触发未经授权运动、重复运动或错误节点运动的帧与脚本；
- 本机路径、设备标识、配置文件或其他敏感信息。

报告中请提供受影响版本、仿真复现、影响范围和建议修复。维护者确认前不要在真实机器人上重复危险操作。

## 边界说明

Action Editor 的 Electron 渲染器采用隔离上下文、严格 CSP、权限拒绝和主进程 IPC 白名单。Python 桥接器仅通过 JSON Lines 与主进程通信，但它不是面向不可信网络开放的服务。任何放宽这些边界的修改都应视为安全敏感变更。
